# assets/

Static assets for project documentation — not part of the shipped `emrac` product.

- [`emrac.png`](./emrac.png) — the project logo/banner shown at the top of the root [`README.md`](../README.md). The README currently embeds it via its hosted GitHub attachment URL rather than this relative path; this file is the source image kept in-repo so it isn't only reachable through that external link.
